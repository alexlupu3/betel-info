{
  "name": "betel-info-monorepo",
  "private": true,
  "scripts": {
    "web:dev":     "npm run dev --prefix apps/web",
    "web:build":   "npm run build --prefix apps/web",
    "web:preview": "npm run preview --prefix apps/web"
  }
}
